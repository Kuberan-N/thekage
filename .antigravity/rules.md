# Rules

* Use App Router (no pages router)
* Keep components reusable
* Avoid inline messy logic
* Always handle empty states
* Use Tailwind cleanly (no clutter)
* Keep code production-ready
